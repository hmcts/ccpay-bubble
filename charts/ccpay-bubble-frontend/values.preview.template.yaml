nodejs:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    CCPAY_BUBBLE_URL: https://${SERVICE_FQDN}
    PAYHUB_API_URL: https://payment-api-pr-830.service.core-compute-preview.internal
    REDISCLOUD_URL: ${SERVICE_NAME}-redis-master
    REDIS_PORT: 6379

idam-pr:
  enabled: true
  redirect_uris:
    pay-bubble:
      - https://${SERVICE_FQDN}/oauth2/callback
redis:
  enabled: true
