nodejs:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    CCPAY_BUBBLE_URL: https://${SERVICE_FQDN}
    CCPAY_REFUNDS_API_URL: https://ccpay-refunds-api-pr-55.service.core-compute-preview.internal

idam-pr:
  enabled: true
  redirect_uris:
    pay-bubble:
      - https://${SERVICE_FQDN}/oauth2/callback
