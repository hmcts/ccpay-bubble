nodejs:
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  environment:
    CCPAY_BUBBLE_URL: https://ccpay-bubble-frontend-pr-325.service.core-compute-preview.internal
    PAYHUB_API_URL: http://payment-api-pr-710.service.core-compute-preview.internal

idam-pr:
  enabled: true
  redirect_uris:
    pay-bubble:
      - https://${SERVICE_FQDN}/oauth2/callback
